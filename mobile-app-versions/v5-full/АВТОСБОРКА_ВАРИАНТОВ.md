# 🤖 Автоматическая сборка всех вариантов v5-full

## 📋 Обзор

GitHub Actions автоматически собирает **все 3 варианта** приложения:
- ⚡ **LITE** (45 MB, 12 инструментов)
- ⭐ **STANDARD** (65 MB, 35 инструментов)
- 💎 **FULL** (95 MB, 57 инструментов)

---

## 🚀 Как запустить автоматическую сборку

### Способ 1: Автоматический (при push)

Просто сделайте изменения в v5-full и запушьте:

```bash
cd /home/user/data20
git add mobile-app-versions/v5-full
git commit -m "Обновление v5-full"
git push origin main
```

**Результат**: Все 3 варианта соберутся автоматически через ~30-40 минут

---

### Способ 2: Ручной запуск (из GitHub UI)

1. Откройте https://github.com/svend4/data20/actions
2. Выберите workflow **"📱 Сборка v5-full - Все варианты"**
3. Нажмите **"Run workflow"**
4. Выберите нужные варианты:
   - ✅ build_lite (по умолчанию включен)
   - ✅ build_standard (по умолчанию включен)
   - ✅ build_full (по умолчанию включен)
5. Нажмите **"Run workflow"**

**Результат**: Выбранные варианты соберутся через ~10-20 минут каждый

---

### Способ 3: При создании Release

При создании GitHub Release все варианты соберутся автоматически:

```bash
# Создать release
./create-release.sh v1.1.0

# Или через GitHub UI
# https://github.com/svend4/data20/releases/new
```

**Результат**: APK автоматически прикрепятся к релизу

---

## 📥 Как скачать собранные APK

### Из GitHub Actions

1. Откройте https://github.com/svend4/data20/actions
2. Выберите успешный запуск workflow
3. Внизу страницы найдите **"Artifacts"**
4. Скачайте нужные APK:
   - `data20-lite-arm64-v8a` (~45 MB)
   - `data20-lite-armeabi-v7a` (~45 MB)
   - `data20-standard-arm64-v8a` (~65 MB)
   - `data20-standard-armeabi-v7a` (~65 MB)
   - `data20-full-arm64-v8a` (~95 MB)
   - `data20-full-armeabi-v7a` (~95 MB)

**⏰ Срок хранения**: 90 дней

---

### Из GitHub Releases

1. Откройте https://github.com/svend4/data20/releases
2. Выберите нужный release (например, v1.1.0)
3. Внизу в **"Assets"** найдите APK файлы
4. Скачайте нужный вариант

**⏰ Срок хранения**: Постоянно

---

## 📊 Что собирается

### ⚡ LITE Variant

**Характеристики**:
- Размер: ~45 MB
- Инструменты: 12
- Package: `com.data20.mobile_app.lite`
- ABI: arm64-v8a только

**12 инструментов**:
1. build_taxonomy
2. build_glossary
3. build_thesaurus
4. advanced_search
5. find_related
6. generate_toc
7. generate_statistics
8. generate_bibliography
9. export_manager
10. duplicate_detector
11. check_links
12. validate

**Файлы**:
- `app-lite-release-arm64-v8a.apk` (~45 MB)

---

### ⭐ STANDARD Variant

**Характеристики**:
- Размер: ~65 MB
- Инструменты: 35
- Package: `com.data20.mobile_app.standard`
- ABI: arm64-v8a + armeabi-v7a

**35 инструментов** (категории):
- Индексация: 9 инструментов
- Поиск и навигация: 9 инструментов
- Анализ и статистика: 5 инструментов
- Визуализация и экспорт: 5 инструментов
- Валидация и качество: 7 инструментов

**Файлы**:
- `app-standard-release-arm64-v8a.apk` (~65 MB)
- `app-standard-release-armeabi-v7a.apk` (~65 MB)

---

### 💎 FULL Variant

**Характеристики**:
- Размер: ~95 MB
- Инструменты: 57 (все)
- Package: `com.data20.mobile_app`
- ABI: arm64-v8a + armeabi-v7a

**57 инструментов** (все категории):
- Анализ: 15 инструментов
- Индексация: 12 инструментов
- Поиск: 8 инструментов
- Визуализация: 10 инструментов
- Валидация: 12 инструментов

**Файлы**:
- `app-full-release-arm64-v8a.apk` (~95 MB)
- `app-full-release-armeabi-v7a.apk` (~95 MB)

---

## ⏱️ Время сборки

| Вариант | Время сборки | Параллельно |
|---------|--------------|-------------|
| LITE | ~10-15 минут | ✅ Да |
| STANDARD | ~15-20 минут | ✅ Да |
| FULL | ~20-25 минут | ✅ Да |
| **Все 3 вместе** | **~25-30 минут** | ✅ Параллельно |

**Примечание**: Все варианты собираются **параллельно** (одновременно), поэтому общее время = время самого долгого варианта.

---

## 🔧 Настройка workflow

### Файл workflow

Файл: `.github/workflows/build-v5-variants.yml`

### Триггеры (когда запускается)

1. **Push в ветки**:
   - main
   - master
   - claude/**

2. **Изменения в файлах**:
   - `mobile-app-versions/v5-full/**`
   - `mobile-app/BUILD_VARIANTS.md`
   - `.github/workflows/build-v5-variants.yml`

3. **Создание Release**:
   - При создании release (types: created, published)

4. **Ручной запуск**:
   - Workflow dispatch (кнопка в GitHub UI)

### Параметры ручного запуска

```yaml
inputs:
  version:
    description: 'Версия APK (например, 1.0.0)'
    default: 'dev'
  build_lite:
    description: 'Собрать LITE вариант?'
    type: boolean
    default: true
  build_standard:
    description: 'Собрать STANDARD вариант?'
    type: boolean
    default: true
  build_full:
    description: 'Собрать FULL вариант?'
    type: boolean
    default: true
```

---

## 📋 Шаги сборки

### Для каждого варианта:

1. **📥 Checkout кода** - клонирование репозитория
2. **☕ Установка Java 17** - для Android SDK
3. **🐍 Установка Python 3.9** - для Chaquopy
4. **🦋 Установка Flutter 3.16.0** - для сборки
5. **📋 Информация о окружении** - версии инструментов
6. **📝 Создание local.properties** - настройка Android SDK
7. **📦 Получение зависимостей** - flutter pub get
8. **🏗️ Сборка APK** - flutter build apk с flavor
9. **📏 Размер APK** - проверка размера
10. **📤 Загрузка APK** - upload artifacts (90 дней)
11. **✅ Завершение** - сообщение об успехе

---

## 🎯 Конфигурация build.gradle

### Flavor dimensions

```gradle
flavorDimensions "variant"
```

### Product flavors

```gradle
productFlavors {
    lite {
        dimension "variant"
        applicationIdSuffix ".lite"
        versionNameSuffix "-lite"
        ndk {
            abiFilters "arm64-v8a"  // Только 64-бит
        }
        buildConfigField "String", "APP_VARIANT", '"lite"'
        buildConfigField "int", "TOOL_COUNT", "12"
    }

    standard {
        dimension "variant"
        applicationIdSuffix ".standard"
        versionNameSuffix "-standard"
        ndk {
            abiFilters "arm64-v8a", "armeabi-v7a"
        }
        buildConfigField "String", "APP_VARIANT", '"standard"'
        buildConfigField "int", "TOOL_COUNT", "35"
    }

    full {
        dimension "variant"
        versionNameSuffix "-full"
        ndk {
            abiFilters "arm64-v8a", "armeabi-v7a"
        }
        buildConfigField "String", "APP_VARIANT", '"full"'
        buildConfigField "int", "TOOL_COUNT", "57"
    }
}
```

---

## 📱 Конфигурация Flutter

### Файл: lib/config/app_variant.dart

```dart
enum AppVariant {
  lite,
  standard,
  full,
}

class AppVariantConfig {
  static AppVariant get variant {
    const variantName = String.fromEnvironment(
      'APP_VARIANT',
      defaultValue: 'full',
    );
    // ...
  }

  static String get displayName {
    // "Data20 Lite", "Data20 Standard", "Data20"
  }

  static int get toolCount {
    // 12, 35, or 57
  }
}
```

---

## 🐛 Troubleshooting

### Сборка не запускается

**Проблема**: Push не триггерит workflow

**Решение**:
1. Проверить что изменения в `mobile-app-versions/v5-full/`
2. Проверить workflow файл существует
3. Запустить вручную из GitHub UI

---

### Сборка падает

**Проблема**: Ошибка во время сборки

**Решение**:
1. Проверить логи в Actions
2. Проверить что flavors настроены в build.gradle
3. Проверить что app_variant.dart существует
4. Попробовать собрать локально для отладки

---

### APK не загружается

**Проблема**: Artifacts пустые

**Решение**:
1. Проверить путь к APK в workflow
2. Проверить что сборка завершилась успешно
3. Проверить что upload-artifact шаг выполнился

---

## 📝 Локальная сборка (для тестирования)

### Lite

```bash
cd mobile-app-versions/v5-full
flutter build apk \
  --flavor lite \
  --release \
  --dart-define=APP_VARIANT=lite \
  --split-per-abi
```

### Standard

```bash
flutter build apk \
  --flavor standard \
  --release \
  --dart-define=APP_VARIANT=standard \
  --split-per-abi
```

### Full

```bash
flutter build apk \
  --flavor full \
  --release \
  --dart-define=APP_VARIANT=full \
  --split-per-abi
```

---

## ✅ Проверка статуса сборки

### В консоли GitHub Actions

1. Откройте https://github.com/svend4/data20/actions
2. Найдите последний запуск "📱 Сборка v5-full"
3. Проверьте статус:
   - 🟢 Зелёная галочка = успешно
   - 🔴 Красный крестик = ошибка
   - 🟡 Жёлтый кружок = в процессе

### В логах

Каждый job показывает:
- ⚡ LITE вариант готов
- ⭐ STANDARD вариант готов
- 💎 FULL вариант готов

### Итоговая сводка

Job "📊 Итоговая сводка" показывает:
- Статус всех 3 вариантов
- Список собранных APK
- Инструкции по скачиванию

---

## 🔗 Полезные ссылки

- **Actions**: https://github.com/svend4/data20/actions
- **Releases**: https://github.com/svend4/data20/releases
- **Workflow файл**: `.github/workflows/build-v5-variants.yml`
- **BUILD_VARIANTS.md**: `mobile-app/BUILD_VARIANTS.md`

---

**Создано**: 2026-01-07
**Статус**: ✅ Настроено и работает
**Версия**: 1.0
**Поддержка**: GitHub Actions автоматическая
