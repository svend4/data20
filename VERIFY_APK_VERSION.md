# ✅ Как проверить версию APK и убедиться что реальные вычисления работают

## 🔍 Шаг 1: Проверка версии установленного APK

### В мобильном приложении:

1. **Запустите приложение**
2. **Запустите backend** (кнопка "Start Backend", подождите 30 секунд)
3. **Откройте раздел "Backend Status"** или любую страницу с информацией о backend

### Что вы должны увидеть в /health endpoint:

#### ✅ **НОВАЯ версия (с реальными вычислениями)**:
```json
{
  "status": "healthy",
  "service": "data20-mobile-backend",
  "version": "simple-0.2.0",          ← ВАЖНО: 0.2.0
  "git_commit": "b25416a",
  "features": {
    "real_implementations": true,     ← ВАЖНО: true
    "database": true,
    "tools_count": 18,
    "implementations_count": 12       ← ВАЖНО: 12
  },
  "message": "Backend is running with real implementations and SQLite database"
}
```

#### ❌ **СТАРАЯ версия (только mock данные)**:
```json
{
  "status": "healthy",
  "service": "data20-mobile-backend",
  "version": "simple-0.1.0",          ← ПРОБЛЕМА: 0.1.0
  "message": "Backend is running (minimal version)"
}
```

---

## 🧪 Шаг 2: Проверка реальных вычислений

### Тест 1: Расчёт статистики

1. Выберите инструмент **"Расчёт статистики"** (calculate_statistics)
2. Введите данные:
   ```json
   {
     "data": [1, 2, 3, 4, 5],
     "metrics": ["mean", "median", "std", "min", "max"]
   }
   ```
3. Нажмите **"Выполнить"**

#### ✅ **Ожидаемый результат (новая версия)**:
```json
{
  "mean": 3.0,
  "median": 3.0,
  "std": 1.4142135623730951,
  "min": 1,
  "max": 5
}
```

#### ❌ **Mock результат (старая версия)**:
```json
{
  "success": true,
  "output": "This is a mock result. Full backend not yet available."
}
```

---

### Тест 2: Анализ текста

1. Выберите инструмент **"Анализ текста"** (text_analysis)
2. Введите:
   ```json
   {
     "text": "Hello world hello",
     "language": "en"
   }
   ```
3. Нажмите **"Выполнить"**

#### ✅ **Ожидаемый результат**:
```json
{
  "total_words": 3,
  "unique_words": 2,
  "total_characters": 17,
  "top_words": [
    {"word": "hello", "count": 2},
    {"word": "world", "count": 1}
  ]
}
```

---

### Тест 3: Base64 кодирование

1. Выберите **"base64_encode"**
2. Введите:
   ```json
   {
     "text": "Hello",
     "operation": "encode"
   }
   ```
3. Результат должен быть: `{"result": "SGVsbG8=", "operation": "encode"}`

---

## 📥 Если версия СТАРАЯ - скачайте новый APK

### Вариант 1: GitHub Actions (рекомендуется)

1. **Откройте**: https://github.com/svend4/data20/actions
2. **Выберите workflow**: "📱 Сборка v5-full - Все варианты"
3. **Найдите последний успешный запуск** (зеленая галочка ✅)
   - Ищите запуск с коммитом **b25416a** или **b8dfcf4**
4. **Скачайте APK** из раздела "Artifacts":
   - `data20-lite-release.apk` (~21 MB)
   - `data20-standard-release.apk` (~21 MB)
   - `data20-full-release.apk` (~21 MB)
5. **Установите на телефон**

### Вариант 2: Запустить сборку вручную

Если workflow не запустился автоматически:

1. **Откройте**: https://github.com/svend4/data20/actions/workflows/build-v5-variants.yml
2. **Нажмите "Run workflow"** (справа)
3. **Выберите**:
   - Branch: `claude/fix-lxml-dependencies-JmbkN`
   - ✅ Build LITE variant
   - ✅ Build STANDARD variant
   - ✅ Build FULL variant
4. **Нажмите "Run workflow"**
5. **Подождите ~45-60 минут** пока соберутся все APK
6. **Скачайте из Artifacts**

---

## 📊 Новые возможности в версии 0.2.0

После установки нового APK у вас будут работать:

### ✅ Реальные вычисления (12 инструментов):
1. **calculate_statistics** - mean, median, std, min, max, variance
2. **text_analysis** - подсчёт слов, символов, топ слова
3. **correlation_analysis** - корреляция Пирсона
4. **word_frequency** - частота слов с рейтингом
5. **json_parser** - парсинг и валидация JSON
6. **csv_parser** - парсинг CSV
7. **base64_encode** - кодирование/декодирование base64
8. **hash_calculator** - MD5, SHA1, SHA256
9. **date_calculator** - вычисления с датами
10. **url_parser** - парсинг URL
11. **outlier_detection** - обнаружение выбросов (IQR метод)
12. **data_filter** - фильтрация данных

### ✅ База данных SQLite:
- История всех выполненных задач
- Сохранение параметров и результатов
- Поиск по истории

### ✅ Новые API endpoints:
- **GET /api/jobs** - история задач
  - `?tool_name=calculate_statistics` - фильтр по инструменту
  - `?limit=50` - лимит результатов
- **GET /api/stats** - статистика использования
  - total_jobs, by_status, top_tools
- **GET /api/search** - поиск инструментов
  - `?q=statistics` - поиск по имени/описанию

---

## 🎯 Итого

1. ✅ **Проверьте /health endpoint** - должно быть `version: "simple-0.2.0"`
2. ✅ **Проверьте вычисления** - должны быть реальные числа
3. ❌ **Если версия старая** - скачайте новый APK из GitHub Actions
4. 🔄 **После установки** - перезапустите приложение и backend

Все изменения уже запушены в ветку `claude/fix-lxml-dependencies-JmbkN`!

Коммиты:
- **b25416a** - Version info in /health (последний)
- **b8dfcf4** - SQLite database
- **7f67df3** - Real implementations
