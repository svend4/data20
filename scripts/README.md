# 🛠️ Scripts - Утилиты для управления Knowledge Base

Эта директория содержит utility scripts для автоматизации работы с 55 инструментами.

---

## 📜 Доступные скрипты

### 1. `generate_all.sh` — Генерация всех outputs

**Главный скрипт** для запуска всех 55 инструментов и генерации HTML/JSON/CSV файлов.

```bash
# Полная генерация (все 55 инструментов)
./scripts/generate_all.sh

# Быстрая генерация (только критичные)
./scripts/generate_all.sh --quick

# Только валидация
./scripts/generate_all.sh --validate-only

# Справка
./scripts/generate_all.sh --help
```

**Возможности:**
- ✅ Приоритизация инструментов (5 тиров)
- ✅ Детальное логирование → `logs/generate_*.log`
- ✅ Подсчёт успехов/ошибок
- ✅ Таймауты для каждого инструмента (5 минут)
- ✅ Цветной вывод прогресса
- ✅ Финальный отчёт с статистикой

**Тиры приоритизации:**
1. **Tier 1** (критичные): `update_indexes.py`, `validate.py`, `metadata_validator.py`
2. **Tier 2** (важные): `build_graph.py`, `search_index.py`, `statistics_dashboard.py`
3. **Tier 3** (контент): `master_index.py`, `export_manager.py`, `archive_builder.py`
4. **Tier 4** (анализ): `duplicate_detector.py`, `check_links.py`, `quality_metrics.py`
5. **Tier 5** (визуализация): `tags_cloud.py`, `timeline_generator.py`

---

### 2. `test_tool.sh` — Тестирование отдельного инструмента

Быстрое тестирование одного инструмента с детальным выводом.

```bash
# Тестировать search_index.py
./scripts/test_tool.sh search_index.py

# Тестировать duplicate_detector.py
./scripts/test_tool.sh duplicate_detector.py
```

**Что делает:**
1. ✅ Проверяет `--help`
2. ✅ Запускает инструмент с таймаутом 60s
3. ✅ Показывает время выполнения
4. ✅ Проверяет созданные файлы
5. ✅ Выводит ошибки с контекстом

---

### 3. `clean_outputs.sh` — Очистка сгенерированных файлов

Удаляет все HTML/JSON/CSV файлы и отчёты.

```bash
# Интерактивная очистка (с подтверждением)
./scripts/clean_outputs.sh

# Принудительная очистка (без вопросов)
./scripts/clean_outputs.sh --force
```

**Удаляет:**
- `*.html` — HTML визуализации
- `*.json` — JSON exports
- `*.csv` — CSV tables
- `*_REPORT.md` — Markdown отчёты
- `*.tex`, `*.svg` — LaTeX и SVG
- Кэш файлы (`.index_cache.json`, `.link_health_cache.json`)

---

## 🚀 Типичные Workflows

### Workflow 1: Полная регенерация

```bash
# 1. Очистить старые файлы
./scripts/clean_outputs.sh --force

# 2. Сгенерировать заново
./scripts/generate_all.sh --full

# 3. Проверить результаты
ls -lh *.html *.json *.csv
```

### Workflow 2: Быстрая проверка изменений

```bash
# Только критичные инструменты (2-3 минуты вместо 20)
./scripts/generate_all.sh --quick
```

### Workflow 3: Отладка конкретного инструмента

```bash
# Протестировать один инструмент
./scripts/test_tool.sh graph_visualizer.py

# Посмотреть подробный лог
tail -f logs/generate_*.log
```

### Workflow 4: Подготовка к commit

```bash
# 1. Валидация
./scripts/generate_all.sh --validate-only

# 2. Если всё ОК, полная генерация
./scripts/generate_all.sh --full

# 3. Commit
git add .
git commit -m "Update knowledge base"
git push
```

---

## 📊 Примеры вывода

### `generate_all.sh` output:

```
╔════════════════════════════════════════════════════════════╗
║   KNOWLEDGE BASE - GENERATOR                               ║
║   55 инструментов для генерации всех outputs              ║
╚════════════════════════════════════════════════════════════╝

======================================
Проверка зависимостей
======================================

✓ Python 3: Python 3.10.12
✓ Python модуль: yaml
✓ Python модуль: pathlib

======================================
TIER 1: 3 инструментов
======================================

[1] Запуск: update_indexes.py (Tier 1)
✓ update_indexes.py завершён успешно

[2] Запуск: validate.py (Tier 1)
✓ validate.py завершён успешно

...

======================================
ОТЧЁТ О ВЫПОЛНЕНИИ
======================================
Всего инструментов: 55
Успешно: 53
Ошибки: 2
Пропущено: 0
Время выполнения: 12m 34s
Лог файл: logs/generate_20260103_122345.log

Сгенерированные файлы:
  HTML: 35 файлов
  JSON: 45 файлов
  CSV: 18 файлов
  Markdown: 22 файлов

✓ Все инструменты выполнены успешно!
```

---

## 🔧 Технические детали

### Структура логов

```
logs/
├── generate_20260103_122345.log   # Полный лог запуска
├── generate_20260103_141523.log   # Следующий запуск
└── ...
```

**Формат лога:**
```
2026-01-03 12:23:45 [INFO] === Generation started ===
2026-01-03 12:23:46 [INFO] Starting tool: update_indexes.py
2026-01-03 12:23:52 [SUCCESS] update_indexes.py completed
2026-01-03 12:23:53 [INFO] Starting tool: validate.py
2026-01-03 12:24:15 [SUCCESS] validate.py completed
...
```

### Обработка ошибок

- **Таймаут**: каждый инструмент имеет лимит 5 минут
- **Код выхода**: скрипт возвращает 0 при успехе, 1 при ошибках
- **Продолжение**: при ошибке одного инструмента, остальные продолжают работу

### Переменные окружения

```bash
# Переопределить директорию с инструментами
TOOLS_DIR=/custom/path ./scripts/generate_all.sh

# Переопределить директорию для outputs
OUTPUT_DIR=/custom/path ./scripts/generate_all.sh
```

---

## 📝 Создание своих скриптов

### Шаблон скрипта:

```bash
#!/bin/bash
set -e  # Остановиться при ошибке

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ROOT_DIR="$(dirname "$SCRIPT_DIR")"
TOOLS_DIR="$ROOT_DIR/tools"

# Ваш код здесь
python3 "$TOOLS_DIR/my_tool.py"
```

### Рекомендации:

1. **Всегда используйте абсолютные пути** (через `$ROOT_DIR`)
2. **Логируйте результаты** в `logs/`
3. **Используйте цветной вывод** для читаемости
4. **Проверяйте зависимости** в начале
5. **Создавайте финальные отчёты**

---

## 🎯 Интеграция с CI/CD

### GitHub Actions example:

```yaml
name: Generate Knowledge Base
on: [push]
jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: pip install pyyaml
      - name: Generate all outputs
        run: ./scripts/generate_all.sh --full
      - name: Upload artifacts
        uses: actions/upload-artifact@v2
        with:
          name: outputs
          path: |
            *.html
            *.json
            *.csv
```

---

## 📚 Дополнительные ресурсы

- **Полная документация**: `/ARCHITECTURE_ANALYSIS.md`
- **Отчёт по проекту**: `/COMPLETE_PROJECT_STATUS.md`
- **Отчёты по тирам**: `/TIER*_REPORT.md`

---

**Создано:** 2026-01-03
**Версия:** 1.0
