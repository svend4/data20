# 📱 Полная инструкция: Публикация Android APK

Это пошаговое руководство по созданию и публикации release APK для Data20 Mobile App.

## 📋 Содержание

1. [Быстрый старт](#быстрый-старт)
2. [Подготовка к релизу](#подготовка-к-релизу)
3. [Создание keystore](#создание-keystore)
4. [Сборка подписанного APK](#сборка-подписанного-apk)
5. [Публикация через GitHub Releases](#публикация-через-github-releases)
6. [Публикация в Google Play Store](#публикация-в-google-play-store)
7. [Автоматизация через GitHub Actions](#автоматизация-через-github-actions)

---

## 🚀 Быстрый старт

### Вариант 1: Неподписанный APK (для тестирования)

```bash
cd mobile-app
./build-android-embedded.sh release
# APK: build/app/outputs/flutter-apk/app-release.apk
```

### Вариант 2: Подписанный APK (для публикации)

```bash
# 1. Создать keystore (только первый раз)
cd mobile-app/android
keytool -genkey -v -keystore data20-release-key.jks \
  -keyalg RSA -keysize 2048 -validity 10000 \
  -alias data20-release

# 2. Создать key.properties
cat > key.properties << 'EOF'
storePassword=ВАШ_ПАРОЛЬ
keyPassword=ВАШ_ПАРОЛЬ
keyAlias=data20-release
storeFile=data20-release-key.jks
EOF

# 3. Собрать подписанный APK
cd ..
./build-android-embedded.sh release
```

---

## 🎯 Подготовка к релизу

### 1. Проверить версию приложения

Откройте `pubspec.yaml` и установите корректную версию:

```yaml
version: 1.0.0+1
# Формат: ВЕРСИЯ_ДЛЯ_ПОЛЬЗОВАТЕЛЕЙ+VERSION_CODE
# Например: 1.0.0+1, 1.0.1+2, 1.1.0+3
```

**Version code** должен увеличиваться с каждым релизом:
- Первый релиз: `1.0.0+1`
- Второй релиз: `1.0.1+2`
- Третий релиз: `1.0.2+3`

### 2. Обновить иконку приложения (опционально)

Если нужно изменить иконку:

```bash
# Поместите иконку 1024x1024 в assets/icon.png
# Затем запустите:
flutter pub run flutter_launcher_icons:main
```

### 3. Протестировать приложение

```bash
# Собрать debug версию
flutter build apk --debug

# Установить на устройство
adb install build/app/outputs/flutter-apk/app-debug.apk

# Протестировать все функции:
# - Вход/выход
# - Backend запуск
# - Инструменты
# - Сохранение данных
```

---

## 🔑 Создание keystore

### Первый релиз

```bash
cd mobile-app/android

# Создать keystore
keytool -genkey -v -keystore data20-release-key.jks \
  -keyalg RSA \
  -keysize 2048 \
  -validity 10000 \
  -alias data20-release
```

**Важные параметры**:
- `keysize 2048` - размер ключа (минимум для Play Store)
- `validity 10000` - срок действия ~27 лет
- `alias data20-release` - имя ключа

**При создании keystore запишите**:
1. ✅ Пароль keystore
2. ✅ Пароль ключа
3. ✅ Имя организации
4. ✅ Город, страна

**⚠️ КРИТИЧЕСКИ ВАЖНО**: Сохраните пароли в безопасном месте! Без них невозможно выпустить обновление приложения.

### Создать key.properties

```bash
# Находясь в mobile-app/android/
cat > key.properties << 'EOF'
storePassword=MySecurePassword123
keyPassword=MySecurePassword123
keyAlias=data20-release
storeFile=data20-release-key.jks
EOF
```

Замените `MySecurePassword123` на ваш реальный пароль.

### Безопасное хранение

```bash
# Создать резервную копию keystore
cp data20-release-key.jks ~/Backups/data20-keystore-backup-$(date +%Y%m%d).jks

# Сохранить в зашифрованном облаке или USB
```

**Рекомендуемые места хранения**:
- ✅ Менеджер паролей (1Password, LastPass)
- ✅ Зашифрованное облако (Dropbox с Cryptomator)
- ✅ USB-накопитель в сейфе
- ❌ НЕ в Git репозитории!

---

## 🏗️ Сборка подписанного APK

### Автоматическая сборка

```bash
cd mobile-app

# Запустить скрипт сборки
./build-android-embedded.sh release

# Ожидайте 10-20 минут (первая сборка)
# APK: build/app/outputs/flutter-apk/app-release.apk
```

Скрипт автоматически:
- ✅ Проверит зависимости
- ✅ Скопирует Python tools
- ✅ Соберет и подпишет APK
- ✅ Покажет размер и путь к APK

### Ручная сборка

```bash
cd mobile-app

# 1. Скопировать tools
./copy-tools-to-python.sh

# 2. Получить зависимости
flutter pub get

# 3. Собрать APK
flutter build apk --release

# APK: build/app/outputs/flutter-apk/app-release.apk
```

### Проверка подписи

```bash
# Проверить что APK подписан
jarsigner -verify -verbose -certs build/app/outputs/flutter-apk/app-release.apk

# Должно быть: "jar verified."
```

Или с помощью apksigner:

```bash
$ANDROID_HOME/build-tools/34.0.0/apksigner verify --print-certs \
  build/app/outputs/flutter-apk/app-release.apk
```

### Проверка размера

```bash
ls -lh build/app/outputs/flutter-apk/app-release.apk

# Ожидаемый размер: ~90-110MB (из-за embedded Python)
```

---

## 📦 Публикация через GitHub Releases

### Способ 1: Через веб-интерфейс

1. **Перейдите на GitHub Releases**:
   ```
   https://github.com/svend4/data20/releases/new
   ```

2. **Заполните форму**:
   - **Tag**: `v1.0.0`
   - **Title**: `📱 Data20 Mobile v1.0.0 - Full Offline Edition`
   - **Description**: Скопируйте из `RELEASE_NOTES.md`

3. **Загрузите APK**:
   - Drag & drop `app-release.apk` в секцию "Attach binaries"
   - Переименуйте в: `data20-mobile-v1.0.0.apk`

4. **Опубликуйте**:
   - ✅ Set as the latest release
   - Нажмите **Publish release**

### Способ 2: Через GitHub CLI

```bash
# Установить GitHub CLI (если нет)
# https://cli.github.com/

# Создать release с APK
gh release create v1.0.0 \
  build/app/outputs/flutter-apk/app-release.apk#data20-mobile-v1.0.0.apk \
  --title "📱 Data20 Mobile v1.0.0 - Full Offline Edition" \
  --notes-file RELEASE_NOTES.md
```

### Способ 3: Автоматически через GitHub Actions

GitHub Actions автоматически соберет и опубликует APK при создании release (см. раздел ниже).

---

## 🏪 Публикация в Google Play Store

### Подготовка

#### 1. Создать аккаунт Google Play Developer

- Стоимость: $25 (одноразовый платеж)
- Регистрация: https://play.google.com/console/signup
- Время активации: 24-48 часов

#### 2. Собрать Android App Bundle (рекомендуется)

```bash
cd mobile-app

# Собрать AAB (вместо APK)
flutter build appbundle --release

# Файл: build/app/outputs/bundle/release/app-release.aab
```

**AAB vs APK**:
- ✅ AAB - рекомендуется Google Play (оптимизация под устройства)
- APK - для прямой установки

#### 3. Подготовить материалы

Создайте следующие файлы:

**Скриншоты** (минимум 2 для каждого типа):
```bash
# Размеры:
# - Телефон: 1080x1920, 1440x2560, 1440x2880
# - 7" планшет: 1200x1920
# - 10" планшет: 1600x2560

# Сделайте скриншоты:
# 1. Главный экран (список инструментов)
# 2. Backend Status экран
# 3. Экран выполнения задачи
# 4. Результаты работы
```

**Иконка приложения**:
- Размер: 512x512 PNG
- Без скруглений (Google добавит автоматически)

**Feature Graphic** (баннер):
- Размер: 1024x500 PNG
- Отображается в топе страницы приложения

**Описание приложения**:
- Краткое (до 80 символов)
- Полное (до 4000 символов)
- Используйте текст из `DOWNLOAD_APK.md`

**Политика конфиденциальности**:
- Обязательна для приложений с аккаунтами
- Разместите на сайте или в GitHub Pages
- URL политики нужно указать при публикации

### Создание приложения в Play Console

1. **Откройте Play Console**:
   ```
   https://play.google.com/console
   ```

2. **Создайте приложение**:
   - Нажмите "Создать приложение"
   - Название: `Data20 Mobile`
   - Язык: Русский
   - Тип: Приложение (не игра)
   - Платное/бесплатное: Бесплатное

3. **Настройте параметры магазина**:

   **Краткое описание** (80 символов):
   ```
   57+ инструментов обработки данных с embedded Python backend. 100% Offline!
   ```

   **Полное описание** (до 4000 символов):
   ```
   📱 Data20 Mobile - Полное Offline Приложение для Анализа Данных

   ✨ Основные возможности:
   • 100% Offline работа - не требует интернета после установки
   • 57+ инструментов анализа, индексации и визуализации данных
   • Встроенный Python Backend (FastAPI + SQLite) на устройстве
   • Modern Material Design 3 UI с темной/светлой темой
   • Полная приватность - все данные локально

   ... (добавьте остальное из DOWNLOAD_APK.md)
   ```

   **Категория**: Инструменты / Продуктивность

4. **Загрузите графику**:
   - Иконка 512x512
   - Feature graphic 1024x500
   - Скриншоты (минимум 2)

5. **Загрузите APK/AAB**:
   - Production → Create new release
   - Upload `app-release.aab`
   - Укажите release notes

6. **Заполните контент-рейтинг**:
   - Ответьте на вопросы о контенте
   - Получите рейтинг (скорее всего "Для всех")

7. **Настройте целевую аудиторию**:
   - Страны: выберите нужные (или "Все страны")
   - Цена: бесплатно

8. **Отправьте на проверку**:
   - Review and publish → Send for review
   - Время проверки: обычно 1-3 дня

### После публикации

- ✅ Приложение появится в Play Store через 1-3 дня
- ✅ Пользователи смогут найти по названию "Data20 Mobile"
- ✅ Обновления публикуются через тот же процесс

---

## 🤖 Автоматизация через GitHub Actions

### Настройка автоматической сборки

Workflow уже настроен в `.github/workflows/build-mobile-apk.yml`, но для подписи APK нужно добавить secrets.

#### 1. Закодировать keystore в base64

```bash
cd mobile-app/android

# macOS
base64 -i data20-release-key.jks | pbcopy

# Linux
base64 -i data20-release-key.jks | xclip -selection clipboard

# Windows (PowerShell)
[Convert]::ToBase64String([IO.File]::ReadAllBytes("data20-release-key.jks")) | clip
```

#### 2. Добавить GitHub Secrets

1. Откройте: https://github.com/svend4/data20/settings/secrets/actions
2. Нажмите "New repository secret"
3. Добавьте следующие secrets:

```
Name: ANDROID_KEYSTORE_BASE64
Value: <вставьте base64 строку>

Name: KEYSTORE_PASSWORD
Value: <ваш keystore password>

Name: KEY_ALIAS
Value: data20-release

Name: KEY_PASSWORD
Value: <ваш key password>
```

#### 3. Обновить workflow

Файл `.github/workflows/build-mobile-apk.yml` уже содержит нужные шаги, но убедитесь что есть:

```yaml
- name: Decode keystore
  if: github.event_name == 'release'
  run: |
    echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 -d > mobile-app/android/data20-release-key.jks

- name: Create key.properties
  if: github.event_name == 'release'
  run: |
    cat > mobile-app/android/key.properties << EOF
    storePassword=${{ secrets.KEYSTORE_PASSWORD }}
    keyPassword=${{ secrets.KEY_PASSWORD }}
    keyAlias=${{ secrets.KEY_ALIAS }}
    storeFile=data20-release-key.jks
    EOF

- name: Build signed APK
  run: |
    cd mobile-app
    flutter build apk --release
```

#### 4. Создать release для запуска workflow

```bash
# Через Git
git tag -a v1.0.0 -m "Release v1.0.0"
git push origin v1.0.0

# Или через GitHub web UI (рекомендуется)
```

GitHub Actions автоматически:
1. Соберет подписанный APK
2. Загрузит его в Release
3. Добавит release notes

---

## 🔄 Обновление приложения

### Выпуск новой версии

1. **Обновите версию** в `pubspec.yaml`:
   ```yaml
   version: 1.0.1+2  # Увеличьте version code!
   ```

2. **Обновите CHANGELOG**:
   ```bash
   echo "## v1.0.1 - $(date +%Y-%m-%d)" >> CHANGELOG.md
   echo "- Fixed bug X" >> CHANGELOG.md
   echo "- Added feature Y" >> CHANGELOG.md
   ```

3. **Соберите новый APK** (тот же keystore!):
   ```bash
   cd mobile-app
   ./build-android-embedded.sh release
   ```

4. **Опубликуйте**:
   - GitHub Releases: создайте новый release `v1.0.1`
   - Google Play: загрузите новый AAB в существующее приложение

**⚠️ ВАЖНО**: Используйте тот же keystore! Иначе обновление будет невозможно.

---

## 📊 Мониторинг после публикации

### Google Play Console

- **Установки**: количество установок и удалений
- **Рейтинги**: оценки пользователей
- **Отзывы**: комментарии пользователей
- **Крэши**: автоматический отчет о падениях
- **ANR**: Android Not Responding события

### GitHub Releases

- **Скачивания**: количество скачиваний каждого релиза
- **Issues**: отзывы через GitHub Issues
- **Discussions**: общение с пользователями

---

## 🐛 Troubleshooting

### Ошибка: "Could not find keystore"

```bash
# Проверьте что keystore существует
ls -la mobile-app/android/data20-release-key.jks

# Проверьте key.properties
cat mobile-app/android/key.properties
```

### Ошибка: "Failed to verify signature"

```bash
# Проверьте пароли в key.properties
# Попробуйте собрать заново
cd mobile-app
flutter clean
./build-android-embedded.sh release
```

### APK не устанавливается

```bash
# Проверьте что APK подписан
jarsigner -verify build/app/outputs/flutter-apk/app-release.apk

# Проверьте версию Android устройства (минимум 7.0)
adb shell getprop ro.build.version.release
```

### Google Play отклонил приложение

**Частые причины**:
- Отсутствует политика конфиденциальности
- Неполное описание
- Недостаточно скриншотов
- Нарушение политики контента

**Решение**: прочитайте комментарии Google и исправьте указанные проблемы.

---

## 📚 Дополнительные ресурсы

- **Android App Signing**: https://developer.android.com/studio/publish/app-signing
- **Flutter Deployment**: https://docs.flutter.dev/deployment/android
- **Google Play Console Help**: https://support.google.com/googleplay/android-developer
- **Play Console Policies**: https://play.google.com/about/developer-content-policy/

---

## ✅ Checklist: Публикация релиза

### Перед сборкой
- [ ] Версия обновлена в pubspec.yaml
- [ ] CHANGELOG.md обновлен
- [ ] Приложение протестировано
- [ ] Keystore создан и сохранен

### Сборка APK
- [ ] Keystore настроен (key.properties)
- [ ] APK собран с подписью
- [ ] Подпись проверена (jarsigner)
- [ ] APK протестирован на устройстве

### GitHub Release
- [ ] Release создан с правильным тегом
- [ ] APK загружен в assets
- [ ] Release notes заполнены
- [ ] Release отмечен как "latest"

### Google Play Store (опционально)
- [ ] AAB собран
- [ ] Play Console аккаунт активен
- [ ] Описание и графика готовы
- [ ] Контент-рейтинг получен
- [ ] Приложение отправлено на проверку

---

**Готово!** Теперь вы можете публиковать Data20 Mobile App! 🚀

Если возникли вопросы - создайте Issue: https://github.com/svend4/data20/issues
