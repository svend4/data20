# 🔍 Как проверить и скачать новые APK с реальными вычислениями

## Проблема
Реальные вычисления не работают, потому что установленные APK содержат **старую версию** backend_main_simple.py без реальных реализаций.

## Решение
Нужно скачать **новые APK** из GitHub Actions, которые содержат обновленный код.

---

## Шаг 1: Проверить статус GitHub Actions

1. **Откройте GitHub**: https://github.com/svend4/data20

2. **Перейдите в Actions**:
   - Нажмите на вкладку **"Actions"** (в верхнем меню)

3. **Найдите workflow**:
   - Выберите workflow: **"📱 Сборка v5-full - Все варианты (Lite, Standard, Full)"**

4. **Проверьте статус последнего запуска**:
   - ✅ **Зеленая галочка** = сборка завершена успешно
   - 🟡 **Желтый кружок** = сборка еще выполняется (подождите ~45-60 минут)
   - ❌ **Красный крестик** = сборка завершилась с ошибкой

---

## Шаг 2: Если workflow НЕ ЗАПУСТИЛСЯ автоматически

### Запустите вручную:

1. **Откройте workflow**:
   - Actions → "📱 Сборка v5-full - Все варианты"

2. **Нажмите "Run workflow"** (справа):
   - Выберите ветку: **`claude/fix-lxml-dependencies-JmbkN`**
   - Отметьте все 3 варианта (lite, standard, full)
   - Нажмите **"Run workflow"**

3. **Подождите завершения**:
   - Lite: ~45 минут
   - Standard: ~50 минут
   - Full: ~60 минут

---

## Шаг 3: Скачать новые APK

### Когда сборка завершена (зеленая галочка ✅):

1. **Откройте завершенный workflow run**:
   - Кликните на запуск с коммитом **"💾 Add SQLite database..."**

2. **Прокрутите вниз до "Artifacts"**:
   - ⚡ **data20-lite-release** - 21 MB (12 инструментов)
   - ⭐ **data20-standard-release** - 21 MB (12 инструментов) 
   - 💎 **data20-full-release** - 21 MB (12 инструментов)

3. **Скачайте нужный вариант**:
   - Artifacts хранятся 90 дней
   - Скачивается как ZIP архив
   - Распакуйте и получите APK файл

4. **Установите на телефон**:
   ```bash
   adb install -r app-lite-release.apk
   # или
   adb install -r app-standard-release.apk
   # или
   adb install -r app-full-release.apk
   ```

---

## Шаг 4: Проверить что реальные вычисления работают

После установки нового APK:

1. **Запустите приложение**
2. **Запустите backend** (подождите 30 сек)
3. **Выберите инструмент "Расчёт статистики"**:
   - Введите данные: `[1, 2, 3, 4, 5]`
   - Выберите метрики: mean, median, std
4. **Нажмите "Выполнить"**

### Ожидаемый результат:
```json
{
  "mean": 3.0,
  "median": 3.0,
  "std": 1.4142135623730951,
  "min": 1,
  "max": 5
}
```

❌ **Если видите mock результат**: APK старый, нужно скачать новый из GitHub Actions

✅ **Если видите реальные числа**: Всё работает правильно!

---

## Коммиты с реальными вычислениями

Новые APK должны содержать эти коммиты:

- ✅ **7f67df3**: Implement real calculations for 12 tools
- ✅ **69b13e7**: Add SQLite database for job history with stats and search

Проверьте, что workflow собрал APK с этими коммитами!

---

## Альтернатива: Локальная сборка (если есть Flutter)

Если GitHub Actions не работает или слишком долго:

```bash
cd /path/to/data20/mobile-app-versions/v5-full

# Lite вариант
flutter build apk --flavor lite --release

# Standard вариант
flutter build apk --flavor standard --release

# Full вариант
flutter build apk --flavor full --release

# APK будут в:
# build/app/outputs/flutter-apk/app-lite-release.apk
# build/app/outputs/flutter-apk/app-standard-release.apk
# build/app/outputs/flutter-apk/app-full-release.apk
```

---

## ❓ Если всё еще не работает

Проверьте логи backend в приложении:
1. Запустите backend
2. В приложении откройте "Backend Status" 
3. Проверьте что видите сообщение: `Database initialized successfully`

Если не видите это сообщение - значит APK старый.
