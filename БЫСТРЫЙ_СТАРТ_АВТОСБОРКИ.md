# ⚡ Быстрый старт: Автоматическая сборка APK

## 🎯 Что настроено

✅ **Автоматическая сборка APK** при каждом push в ветки `main`, `master` или `claude/**`
✅ **Ручной запуск** через GitHub Actions UI
✅ **Автоматическая публикация** в GitHub Releases
✅ **90 дней хранения** APK в Artifacts

## 🚀 3 способа получить APK

### 1️⃣ Автоматически (Рекомендуется)

```bash
# Просто сделайте изменения в mobile-app/ и запушьте
cd mobile-app
# ... внесите изменения ...
git add .
git commit -m "Update mobile app"
git push origin claude/automate-apk-build-fG2Qa
```

**Результат**: APK автоматически соберется через ~10 минут

### 2️⃣ Вручную через GitHub UI

1. Откройте: https://github.com/svend4/data20/actions
2. Выберите **"🤖 Автоматическая сборка Android APK"**
3. Нажмите **"Run workflow"** → **"Run workflow"**

**Результат**: APK соберется и будет доступен в Artifacts

### 3️⃣ Создание Release

```bash
gh release create v1.0.0 --title "Версия 1.0.0" --notes "Описание"
```

**Результат**: APK автоматически добавится в Release

## 📥 Где скачать готовый APK

### Вариант A: Из Actions Artifacts (быстрее)

1. https://github.com/svend4/data20/actions
2. Выберите последнюю успешную сборку (зеленая галочка ✅)
3. Прокрутите вниз до **Artifacts**
4. Скачайте `data20-mobile-XXX.apk`

### Вариант B: Из Releases (официальные версии)

1. https://github.com/svend4/data20/releases
2. Выберите последний релиз
3. Скачайте APK из **Assets**

## 🔐 Настройка подписи APK (Опционально)

Для подписанного Release APK нужно добавить секреты в GitHub:

```bash
# 1. Создать keystore
cd mobile-app/android
keytool -genkey -v -keystore data20-release-key.jks \
  -keyalg RSA -keysize 2048 -validity 10000 -alias data20

# 2. Кодировать в base64
base64 -i data20-release-key.jks -o keystore.base64

# 3. Добавить в GitHub Settings → Secrets:
#    ANDROID_KEYSTORE_BASE64 = содержимое keystore.base64
#    KEYSTORE_PASSWORD = ваш пароль
#    KEY_ALIAS = data20
#    KEY_PASSWORD = ваш пароль ключа
```

## 📊 Что проверить

После запуска workflow проверьте:

- ✅ Workflow запустился (зеленый статус)
- ✅ Все шаги выполнены успешно
- ✅ APK появился в Artifacts
- ✅ Размер APK ~100MB

## ❗ Частые вопросы

**Q: Сколько времени занимает сборка?**
A: ~8-12 минут

**Q: Можно ли собрать APK локально?**
A: Да, используйте скрипт: `cd mobile-app && ./build-android-embedded.sh release`

**Q: Зачем нужен keystore?**
A: Для подписи официальных релизов. Для тестирования не обязателен.

**Q: APK не собирается, что делать?**
A: Проверьте логи в Actions → выберите упавшую сборку → смотрите ошибки

## 📚 Подробная документация

- [АВТОСБОРКА_APK.md](АВТОСБОРКА_APK.md) - полное руководство
- [mobile-app/BUILD_MOBILE_EMBEDDED.md](mobile-app/BUILD_MOBILE_EMBEDDED.md) - сборка локально
- [mobile-app/KEYSTORE_SETUP.md](mobile-app/KEYSTORE_SETUP.md) - настройка подписи

## 🎉 Готово!

Теперь APK будет собираться автоматически при каждом push. Просто пушьте код и получайте готовый APK!

---

**Следующий шаг**: Сделайте push в вашу ветку и проверьте Actions → APK соберется автоматически! 🚀
