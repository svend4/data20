# 🧪 Mobile App Sandboxes - Экспериментальные версии

Эта директория содержит **песочницы** (sandboxes) для экспериментов с разными версиями mobile-app.

---

## 📋 Назначение

После анализа изменений в Phase 7.3 (см. `docs/POST_7_3_CHANGES_REPORT.md`) были обнаружены:
- ✅ Позитивные изменения: модульная архитектура, все 57 инструментов
- ❌ Потери функционала: `run_server_async()`, `stop_server()`, `create_mobile_app()`, `initialize_database()`

Эти песочницы позволяют:
1. **Сохранить оригинальный код** из разных этапов Phase 7.3
2. **Экспериментировать с восстановлением** потерянных функций
3. **Тестировать разные подходы** к сборке APK
4. **Не трогать основной код** в `mobile-app/` и `mobile-app-versions/`

---

## 📁 Структура

```
mobile-app-sandboxes/
├── README.md                     # Этот файл
├── original-ca458ea/             # Оригинал Phase 7.3 FULL IMPLEMENTATION
├── current-324dd58/              # Текущий Phase 7.3 READY FOR APK
├── hybrid-best-of-both/          # Гибрид: лучшее из обоих версий
└── build-experiments/            # Эксперименты со сборкой
    ├── experiment-1/             # Эксперимент 1
    ├── experiment-2/             # Эксперимент 2
    └── experiment-3/             # Эксперимент 3
```

---

## 🎯 Песочницы

### 1. original-ca458ea (Оригинал)

**Commit**: ca458ea
**Название**: Phase 7.3: Mobile Embedded Backend - FULL IMPLEMENTATION
**Дата**: ~2026-01-03

#### Что включено:
- ✅ `backend_main.py` с **всеми функциями**:
  - `create_mobile_app()` - создание FastAPI app
  - `run_server_async()` - **запуск в фоне**
  - `stop_server()` - **остановка сервера**
  - `initialize_database()` - **инициализация БД**
- ✅ Полный набор environment variables
- ✅ Конфигурация (DEBUG, CORS, ENABLE_CELERY и т.д.)

#### Что НЕ включено:
- ❌ Модули: mobile_server.py, mobile_auth.py и т.д.
- ❌ 57 инструментов в tools/
- ❌ copy-tools-to-python.sh скрипт

#### Использование:
```bash
cd mobile-app-sandboxes/original-ca458ea

# Посмотреть функции в backend_main.py
grep "^def " android/app/src/main/python/backend_main.py

# Найти: run_server_async, stop_server, create_mobile_app, initialize_database
```

#### Когда использовать:
- Нужно восстановить потерянные функции
- Изучить оригинальную архитектуру
- Понять как работал `run_server_async()`

---

### 2. current-324dd58 (Текущий)

**Commit**: 324dd58
**Название**: Phase 7.3: READY FOR APK DOWNLOAD - Complete Backend Integration
**Дата**: ~2026-01-03

#### Что включено:
- ✅ **Модульная архитектура**:
  - mobile_server.py (+427 строк)
  - mobile_auth.py (+157 строк)
  - mobile_database.py (+81 строк)
  - mobile_models.py (+351 строк)
  - mobile_tool_registry.py (+489 строк)
  - mobile_tool_runner.py (+311 строк)
- ✅ **Все 57 инструментов** в tools/
- ✅ copy-tools-to-python.sh скрипт
- ✅ `init_mobile_database()` в mobile_database.py

#### Что НЕ включено:
- ❌ `run_server_async()` - только `run_server()` (блокирующий)
- ❌ `stop_server()` - нет функции остановки
- ❌ `create_mobile_app()` - импорт из mobile_server
- ❌ Некоторые environment variables

#### Использование:
```bash
cd mobile-app-sandboxes/current-324dd58

# Посмотреть модульную архитектуру
ls -la android/app/src/main/python/mobile_*.py

# Посмотреть все 57 инструментов
ls android/app/src/main/python/tools/ | wc -l  # Должно быть 57
```

#### Когда использовать:
- Текущая рабочая версия для сборки APK
- Нужна модульная архитектура
- Нужны все 57 инструментов

---

### 3. hybrid-best-of-both (Гибрид) ⭐

**Основа**: 324dd58 (current) + восстановленные функции из ca458ea
**Статус**: ⚙️ В процессе создания

#### Цель:
Объединить **лучшее из обоих миров**:
- ✅ Модульная архитектура (mobile_server.py и т.д.) из 324dd58
- ✅ Все 57 инструментов из 324dd58
- ✅ Восстановленные функции из ca458ea:
  - `run_server_async()` - запуск в фоне
  - `stop_server()` - остановка сервера
  - `create_mobile_app()` - альтернативный способ создания app
  - `initialize_database()` - дополнительная инициализация

#### План реализации:
1. Взять backend_main.py из 324dd58 как основу
2. Добавить `run_server_async()` из ca458ea
3. Добавить `stop_server()` из ca458ea
4. Добавить `create_mobile_app()` как вспомогательную функцию
5. Протестировать совместимость

#### Использование:
```bash
cd mobile-app-sandboxes/hybrid-best-of-both

# После реализации:
# Проверить что есть все функции
grep "^def " android/app/src/main/python/backend_main.py

# Должны быть:
# - setup_environment
# - run_server
# - run_server_async  ← Восстановлена!
# - stop_server       ← Восстановлена!
```

#### Когда использовать:
- Нужна вся функциональность (модули + async функции)
- Тестирование background запуска сервера
- Разработка механизма остановки сервера

---

### 4. build-experiments/ (Эксперименты)

Директория для экспериментов со сборкой APK.

#### experiment-1: Попытка сборки с минимальными зависимостями

**Идея**: Уменьшить размер APK убрав ненужные пакеты
**План**:
- Удалить pandas, numpy из requirements.txt
- Оставить только FastAPI, uvicorn, SQLAlchemy
- Попробовать собрать APK

**Статус**: ⏳ Ожидает реализации

---

#### experiment-2: Попытка сборки с Chaquopy оптимизацией

**Идея**: Оптимизировать Chaquopy настройки в build.gradle
**План**:
- Изменить buildPython настройки
- Включить strip mode для уменьшения размера
- Попробовать собрать APK

**Статус**: ⏳ Ожидает реализации

---

#### experiment-3: Попытка сборки без некоторых инструментов

**Идея**: Собрать APK только с основными инструментами (как v3-lite)
**План**:
- Оставить только 12 инструментов
- Уменьшить размер APK до ~50MB
- Попробовать собрать APK

**Статус**: ⏳ Ожидает реализации

---

## 🚀 Как использовать

### Просмотр содержимого песочниц

```bash
# Посмотреть оригинальную версию
cd mobile-app-sandboxes/original-ca458ea
cat android/app/src/main/python/backend_main.py | grep "def "

# Посмотреть текущую версию
cd mobile-app-sandboxes/current-324dd58
ls -la android/app/src/main/python/

# Сравнить версии
diff -r original-ca458ea/android/app/src/main/python/backend_main.py \
        current-324dd58/android/app/src/main/python/backend_main.py
```

### Эксперименты

```bash
# Создать новый эксперимент
mkdir mobile-app-sandboxes/build-experiments/experiment-4

# Скопировать базу из текущей версии
cp -r mobile-app-sandboxes/current-324dd58/* \
      mobile-app-sandboxes/build-experiments/experiment-4/

# Внести изменения
cd mobile-app-sandboxes/build-experiments/experiment-4
# ... ваши изменения ...

# Попробовать собрать
./build-android-embedded.sh release
```

### Восстановление функций

```bash
# Скопировать функцию из оригинала в гибрид
cd mobile-app-sandboxes

# Посмотреть функцию run_server_async в оригинале
grep -A 15 "def run_server_async" \
    original-ca458ea/android/app/src/main/python/backend_main.py

# Скопировать в гибрид
# ... manual copy or use patch ...
```

---

## 📊 Сравнение версий

| Песочница | Размер | Модули | Инструменты | run_server_async | stop_server | Сборка APK |
|-----------|--------|--------|-------------|------------------|-------------|------------|
| **original-ca458ea** | ~300 строк | ❌ Нет | ❌ 0 | ✅ Есть | ✅ Есть | ❌ Не готова |
| **current-324dd58** | ~55,700 строк | ✅ 6 модулей | ✅ 57 | ❌ Нет | ❌ Нет | ✅ Готова |
| **hybrid** | ~55,800 строк | ✅ 6 модулей | ✅ 57 | ⚙️ Восстановить | ⚙️ Восстановить | ⚙️ Тестировать |

---

## ⚠️ Важные правила

### 1. НЕ ТРОГАТЬ основной код

❌ **НЕ ИЗМЕНЯТЬ**:
- `mobile-app/` - основная рабочая директория
- `mobile-app-versions/` - версии для пользователей

✅ **ИЗМЕНЯТЬ МОЖНО**:
- `mobile-app-sandboxes/` - только для экспериментов

### 2. Каждый эксперимент - отдельная папка

Для каждой попытки сборки/изменения создавайте **новую папку** в `build-experiments/`:

```bash
mkdir mobile-app-sandboxes/build-experiments/experiment-5-my-test
# Делайте эксперименты там
```

### 3. Документирование изменений

В каждом эксперименте создавайте `README.md`:

```markdown
# Experiment 5: My Test

## Цель
Что пытаетесь сделать

## Изменения
Какие файлы изменили

## Результат
Что получилось / не получилось

## Выводы
Что узнали
```

---

## 🔄 Workflow для экспериментов

### Пример: Добавление run_server_async в current версию

```bash
# Шаг 1: Создать новый эксперимент
mkdir mobile-app-sandboxes/build-experiments/add-async-functions
cd mobile-app-sandboxes/build-experiments/add-async-functions

# Шаг 2: Скопировать текущую версию
cp -r ../../current-324dd58/* ./

# Шаг 3: Скопировать функцию из оригинала
# Открыть в редакторе:
# - ../../original-ca458ea/android/app/src/main/python/backend_main.py
# - ./android/app/src/main/python/backend_main.py
# Скопировать функции run_server_async() и stop_server()

# Шаг 4: Тестировать
# (если есть Flutter SDK и Android SDK)
flutter pub get
flutter build apk --release

# Шаг 5: Документировать
echo "## Результат
Добавлены run_server_async() и stop_server().
Сборка прошла успешно / не прошла.
" > README.md

# Шаг 6: Если успешно - переместить в hybrid-best-of-both
cp android/app/src/main/python/backend_main.py \
   ../../hybrid-best-of-both/android/app/src/main/python/
```

---

## 📚 Связанная документация

- **Полный отчет об изменениях**: `docs/POST_7_3_CHANGES_REPORT.md`
- **Технологический анализ**: `docs/TECH_LEVELS_ANALYSIS.md`
- **Аудит проекта**: `docs/GIT_AUDIT_FULL.md`
- **Версии для пользователей**: `mobile-app-versions/README.md`

---

## 🎯 Цели песочниц

1. ✅ **Сохранить оригинальный код** - original-ca458ea как reference
2. ✅ **Сохранить текущий код** - current-324dd58 как baseline
3. ⏳ **Создать гибрид** - hybrid-best-of-both с лучшими частями
4. ⏳ **Экспериментировать** - build-experiments для тестирования
5. ⏳ **Документировать** - записывать все изменения

---

**Создано**: 2026-01-05
**Назначение**: Песочницы для экспериментов, не трогая основной код
**Политика**: Все эксперименты только в этой директории

**Экспериментируйте смело!** 🧪
