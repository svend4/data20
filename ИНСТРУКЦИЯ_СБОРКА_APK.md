# 🚀 Инструкция: Как запустить сборку новых APK с реальными вычислениями

## ❗ Проблема

Мобильное приложение не получает обновления, потому что:
- ✅ Код обновлен в репозитории (версия backend 0.2.0)
- ❌ APK файлы НЕ пересобраны с новым кодом
- ❌ Установленный APK содержит старую версию (backend 0.1.0)

## ✅ Решение: Запустить сборку APK

---

## 📱 Шаг 1: Запуск workflow сборки

### Откройте GitHub Actions:

1. **Перейдите на GitHub**: https://github.com/svend4/data20

2. **Откройте вкладку "Actions"** (в верхнем меню)

3. **Выберите workflow**:
   ```
   📱 Сборка v5-full - Все варианты (Lite, Standard, Full)
   ```

4. **Нажмите кнопку "Run workflow"** (справа, зеленая кнопка)

### Настройки запуска:

```
Branch:           claude/fix-lxml-dependencies-JmbkN
                  (ВАЖНО: выберите именно эту ветку!)

Build LITE:       ✅ (отмечено)
Build STANDARD:   ✅ (отмечено)
Build FULL:       ✅ (отмечено)
```

5. **Нажмите "Run workflow"** (зеленая кнопка внизу)

---

## ⏱️ Шаг 2: Ожидание завершения сборки

Время сборки:
- ⚡ **LITE**: ~45 минут
- ⭐ **STANDARD**: ~50 минут
- 💎 **FULL**: ~60 минут

**Общее время**: примерно 1 час (все 3 варианта собираются параллельно)

### Как отследить прогресс:

1. **Обновите страницу Actions**
2. **Найдите запущенный workflow** (желтый кружок 🟡 = выполняется)
3. **Дождитесь зеленой галочки** ✅ = завершено успешно

---

## 📥 Шаг 3: Скачивание новых APK

После успешного завершения (✅):

1. **Откройте завершенный workflow run**
   - Кликните на запуск с зеленой галочкой

2. **Прокрутите вниз до раздела "Artifacts"**

3. **Скачайте нужные APK**:
   - ⚡ **data20-lite-release** (~21 MB) - 12 инструментов
   - ⭐ **data20-standard-release** (~21 MB) - 12 инструментов
   - 💎 **data20-full-release** (~21 MB) - 12 инструментов

4. **Распакуйте ZIP архив** → получите APK файл

---

## 📲 Шаг 4: Установка нового APK

### На телефоне:

1. **Удалите старое приложение** (чтобы избежать конфликтов)
2. **Скопируйте APK на телефон**
3. **Установите APK** (разрешите установку из неизвестных источников)

### Через ADB (если подключен к компьютеру):

```bash
# Установка с заменой (сохраняет данные)
adb install -r app-lite-release.apk

# или
adb install -r app-standard-release.apk

# или
adb install -r app-full-release.apk
```

---

## ✅ Шаг 5: Проверка обновлений

После установки нового APK:

### 1. Запустите приложение

### 2. Запустите backend
- Нажмите кнопку **"Start Backend"**
- Подождите **30 секунд**

### 3. Проверьте версию
- Откройте раздел **"Backend Status"**
- Или сделайте GET запрос: `http://localhost:8080/health`

### Должны увидеть:

```json
{
  "status": "healthy",
  "service": "data20-mobile-backend",
  "version": "simple-0.2.0",          ← ВАЖНО!
  "git_commit": "b8dfcf4",
  "features": {
    "real_implementations": true,     ← ВАЖНО!
    "database": true,
    "tools_count": 18,
    "implementations_count": 12       ← ВАЖНО!
  },
  "message": "Backend is running with real implementations and SQLite database"
}
```

❌ **Если видите версию 0.1.0** → APK старый, повторите установку

✅ **Если видите версию 0.2.0** → Всё работает!

---

## 🧪 Шаг 6: Проверка реальных вычислений

Протестируйте любой инструмент:

### Тест: Расчёт статистики

1. Выберите инструмент **"Расчёт статистики"** (calculate_statistics)

2. Введите данные:
   ```json
   {
     "data": [1, 2, 3, 4, 5],
     "metrics": ["mean", "median", "std", "min", "max"]
   }
   ```

3. Нажмите **"Выполнить"**

4. **Ожидаемый результат** (новая версия):
   ```json
   {
     "mean": 3.0,
     "median": 3.0,
     "std": 1.4142135623730951,
     "min": 1,
     "max": 5
   }
   ```

❌ **Mock результат** (старая версия):
```json
{
  "success": true,
  "output": "This is a mock result. Full backend not yet available."
}
```

---

## 📊 Что нового в версии 0.2.0

### ✅ Реальные вычисления (12 инструментов):
1. **calculate_statistics** - mean, median, std, min, max, variance
2. **text_analysis** - подсчёт слов, символов, топ слова
3. **correlation_analysis** - корреляция Пирсона
4. **word_frequency** - частота слов с рейтингом
5. **json_parser** - парсинг и валидация JSON
6. **csv_parser** - парсинг CSV
7. **base64_encode** - кодирование/декодирование base64
8. **hash_calculator** - MD5, SHA1, SHA256
9. **date_calculator** - вычисления с датами
10. **url_parser** - парсинг URL
11. **outlier_detection** - обнаружение выбросов (IQR метод)
12. **data_filter** - фильтрация данных

### ✅ База данных SQLite:
- История всех выполненных задач
- Сохранение параметров и результатов
- Поиск по истории

### ✅ Новые API endpoints:
- **GET /api/jobs** - история задач
  - `?tool_name=calculate_statistics` - фильтр по инструменту
  - `?limit=50` - лимит результатов
- **GET /api/stats** - статистика использования
  - total_jobs, by_status, top_tools
- **GET /api/search** - поиск инструментов
  - `?q=statistics` - поиск по имени/описанию

---

## 🎯 Краткая схема

```
1. GitHub → Actions → "📱 Сборка v5-full" → Run workflow
   ↓
2. Branch: claude/fix-lxml-dependencies-JmbkN
   ↓
3. Ждать ~1 час (все 3 APK соберутся параллельно)
   ↓
4. Actions → Artifacts → Скачать APK
   ↓
5. Установить на телефон
   ↓
6. Запустить приложение → Start Backend
   ↓
7. Проверить /health → version: "simple-0.2.0" ✅
```

---

## ❓ Частые вопросы

### Q: Почему приложение не обновляется автоматически?
**A:** Это не обновление по воздуху (OTA). Нужно установить новый APK файл вручную.

### Q: Можно ли обновить без удаления старого приложения?
**A:** Да, используйте `adb install -r` или просто установите поверх (но рекомендуется удалить старое).

### Q: Как долго хранятся APK в Artifacts?
**A:** 90 дней с момента сборки.

### Q: Почему workflow не запустился автоматически?
**A:** Workflow запускается только при push в ветки с изменениями в `mobile-app-versions/v5-full/**`. Изменения были замержены через PR, поэтому нужно запустить вручную.

### Q: Какую версию выбрать: Lite, Standard или Full?
**A:**
- **Lite** - минимальная версия, 12 инструментов (рекомендуется для тестирования)
- **Standard** - средняя версия, 12 инструментов (пока то же самое что Lite)
- **Full** - полная версия, 12 инструментов (в будущем будет 57 инструментов)

Для проверки реальных вычислений подходит любая версия.

---

## 🔧 Альтернатива: Локальная сборка

Если GitHub Actions не работает или занимает слишком много времени:

```bash
# Клонировать репозиторий
git clone https://github.com/svend4/data20.git
cd data20

# Переключиться на нужную ветку
git checkout claude/fix-lxml-dependencies-JmbkN

# Перейти в директорию проекта
cd mobile-app-versions/v5-full

# Установить Flutter (если еще не установлен)
# https://docs.flutter.dev/get-started/install

# Установить зависимости
flutter pub get

# Собрать APK - выберите нужный вариант:

# LITE вариант
flutter build apk --flavor lite --release

# STANDARD вариант
flutter build apk --flavor standard --release

# FULL вариант
flutter build apk --flavor full --release

# APK будут находиться в:
# build/app/outputs/flutter-apk/app-lite-release.apk
# build/app/outputs/flutter-apk/app-standard-release.apk
# build/app/outputs/flutter-apk/app-full-release.apk
```

**Требования для локальной сборки:**
- Flutter SDK 3.16.0+
- Java JDK 17
- Android SDK
- Python 3.9+ (для Chaquopy)

---

## 📝 Резюме

**Проблема:** Установленный APK содержит старый backend без реальных вычислений

**Решение:**
1. Запустить workflow сборки на ветке `claude/fix-lxml-dependencies-JmbkN`
2. Скачать новый APK из Artifacts
3. Установить на телефон
4. Проверить версию 0.2.0 в /health endpoint

**Результат:** Мобильное приложение получит все новые функции:
- ✅ Реальные вычисления (12 инструментов)
- ✅ База данных SQLite
- ✅ Новые API endpoints
- ✅ 18 инструментов вместо 6

---

## 📞 Помощь

Если возникли проблемы:
1. Проверьте логи workflow в GitHub Actions
2. Проверьте версию backend в /health endpoint
3. Убедитесь что установили APK из правильного Artifact
4. Перезапустите backend в приложении

**Важно:** Workflow должен быть запущен на ветке `claude/fix-lxml-dependencies-JmbkN`, иначе APK будет без новых функций!
