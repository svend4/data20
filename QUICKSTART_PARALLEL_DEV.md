# ⚡ Быстрый старт: Параллельное развитие версий

**5-минутное руководство по использованию инструментов синхронизации**

---

## 🎯 Основная идея

```
Улучшил в одном месте → Автоматически распространил на все 7 версий!
```

---

## 🛠️ Инструменты

### 1. `sync-versions.sh` - Синхронизация файлов

**Синтаксис:**
```bash
./sync-versions.sh [компонент] [источник] [опции]
```

**Быстрые команды:**

```bash
# Синхронизировать backend_main.py из hybrid во все версии
./sync-versions.sh backend_main hybrid --force

# Синхронизировать все Gradle конфиги из v5-full
./sync-versions.sh gradle v5-full --force

# Синхронизировать всё из v5-full (предпросмотр)
./sync-versions.sh all v5-full --dry-run

# Синхронизировать всё из v5-full (применить)
./sync-versions.sh all v5-full --force
```

**Компоненты:**
- `all` - всё сразу
- `gradle` - Gradle конфиги (5 файлов)
- `backend` - Python backend (7 файлов)
- `flutter` - Flutter/Dart код
- `backend_main` - только backend_main.py
- `mobile_server` - только mobile_server.py
- `storage_service` - только storage_service.dart

---

### 2. `check-consistency.sh` - Проверка консистентности

**Использование:**
```bash
./check-consistency.sh
```

**Что проверяет:**
- ✅ Количество функций в backend_main.py
- ✅ Версии Gradle плагинов
- ✅ Наличие settings.gradle
- ✅ Наличие Android ресурсов
- ✅ Наличие Flutter зависимостей

**Вывод:**
```
✅ Полностью синхронизировано: 7 версий
⚠️  Частично синхронизировано: 0 версий
❌ Не синхронизировано: 0 версий
```

---

## 🚀 Типичные сценарии

### Сценарий 1: Добавил новую функцию в hybrid

```bash
# 1. Разработка в hybrid
cd mobile-app-sandboxes/hybrid-best-of-both
vim android/app/src/main/python/backend_main.py
# Добавили новую функцию

# 2. Тест локально
python3 android/app/src/main/python/backend_main.py --debug
# ✅ Работает!

# 3. Синхронизация в v5-full
cd ../../..
./sync-versions.sh backend_main hybrid

# 4. Синхронизация во все версии
./sync-versions.sh backend_main v5-full --force

# 5. Проверка
./check-consistency.sh

# 6. Коммит
git add .
git commit -m "✨ [ALL] Добавлена новая функция из hybrid"
git push
```

---

### Сценарий 2: Обновил Gradle плагины в v5-full

```bash
# 1. Обновление в v5-full
vim mobile-app-versions/v5-full/android/build.gradle
# Обновили Chaquopy до 15.0.2

# 2. Тест сборки
cd mobile-app-versions/v5-full
flutter build apk --flavor lite --release
# ✅ Собралось!

# 3. Синхронизация во все версии
cd ../..
./sync-versions.sh gradle v5-full --force

# 4. Проверка
./check-consistency.sh

# 5. Коммит
git add mobile-app-versions/
git commit -m "⬆️ [ALL] Обновление Gradle плагинов до latest"
git push
```

---

### Сценарий 3: Исправил bug в Flutter коде

```bash
# 1. Исправление в v5-full
vim mobile-app-versions/v5-full/lib/services/storage_service.dart
# Исправили импорт

# 2. Синхронизация во все версии
./sync-versions.sh storage_service v5-full --force

# 3. Проверка
./check-consistency.sh

# 4. Коммит
git add mobile-app-versions/*/lib/services/storage_service.dart
git commit -m "🐛 [ALL] Исправлен импорт в storage_service"
git push
```

---

### Сценарий 4: Полная синхронизация всего из v5-full

```bash
# Предпросмотр изменений
./sync-versions.sh all v5-full --dry-run

# Применить всё
./sync-versions.sh all v5-full --force

# Проверка
./check-consistency.sh
# Должно показать: ✅ Полностью синхронизировано: 7 версий

# Коммит
git add mobile-app-versions/
git commit -m "🔄 [ALL] Полная синхронизация из v5-full"
git push
```

---

## 📋 Чеклист после синхронизации

```bash
# 1. Проверить консистентность
./check-consistency.sh

# 2. Выборочно протестировать сборку
cd mobile-app-versions/v3-lite
flutter build apk --release
# ✅ Собралось!

cd ../v4-standard
flutter build apk --release
# ✅ Собралось!

# 3. Проверить git diff
cd ../..
git diff mobile-app-versions/

# 4. Закоммитить
git add mobile-app-versions/
git commit -m "🔄 [ALL] Синхронизация компонентов"
git push
```

---

## 🤖 Автоматическая синхронизация через GitHub Actions

### Workflow активируется автоматически при:
- Push в v5-full
- Push в hybrid-best-of-both

### Или запустить вручную:
1. Открыть https://github.com/svend4/data20/actions
2. Выбрать workflow "🔄 Auto-Sync Versions"
3. Нажать "Run workflow"
4. Выбрать параметры:
   - Source: `v5-full` или `hybrid`
   - Component: `all`, `gradle`, `backend`, `flutter`
   - Dry run: `true` (предпросмотр) или `false` (применить)

---

## 📊 Мониторинг статуса

### Быстрая проверка:

```bash
# Сколько функций в backend_main.py у каждой версии
for v in mobile-app-versions/v*/android/app/src/main/python/backend_main.py; do
    echo "$(basename $(dirname $(dirname $(dirname $(dirname $v))))):" $(grep -c "^def " "$v" 2>/dev/null || echo "0")
done

# Должно быть у всех одинаково (например 8 функций)
```

### Проверка версий Gradle:

```bash
# Версия Chaquopy у каждой версии
grep -h "chaquo.python:gradle:" mobile-app-versions/*/android/build.gradle 2>/dev/null | sort | uniq -c

# Должна быть одна версия у всех (например 15.0.1)
```

---

## 💡 Советы

### ✅ DO:
- Всегда делать `--dry-run` сначала
- Проверять через `check-consistency.sh` после синхронизации
- Тестировать хотя бы 1-2 версии после изменений
- Коммитить с префиксом `[ALL]` для отслеживания

### ❌ DON'T:
- Не синхронизировать без тестирования источника
- Не пропускать проверку консистентности
- Не удалять функции при синхронизации
- Не менять API без backwards compatibility

---

## 🎓 Формулы коммитов

```bash
# Новая функция
git commit -m "✨ [ALL] Добавлена функция get_metrics()

Источник: hybrid-best-of-both
Синхронизировано в: v1-v7"

# Bug fix
git commit -m "🐛 [ALL] Исправлен импорт shared_preferences

Источник: v5-full
Синхронизировано в: v1-v7"

# Обновление зависимостей
git commit -m "⬆️ [ALL] Обновление Chaquopy до 15.0.2

Источник: v5-full
Синхронизировано в: v1-v7"

# Полная синхронизация
git commit -m "🔄 [ALL] Полная синхронизация из v5-full

Компоненты: gradle, backend, flutter, android_res
Синхронизировано в: v1-v7"
```

---

## 🚨 Решение проблем

### Проблема: Скрипт не находит файл

```bash
# Проверить путь к файлу в источнике
ls -la mobile-app-versions/v5-full/android/app/src/main/python/backend_main.py

# Если файл есть, проверить права
chmod +x sync-versions.sh
```

### Проблема: Изменения не применяются

```bash
# Использовать --force для перезаписи
./sync-versions.sh backend_main v5-full --force
```

### Проблема: Не все версии синхронизированы

```bash
# Проверить статус
./check-consistency.sh

# Синхронизировать всё принудительно
./sync-versions.sh all v5-full --force
```

---

## 📚 Дополнительная документация

- **Полная стратегия:** `PARALLEL_DEVELOPMENT_STRATEGY.md`
- **Анализ песочниц:** `SANDBOXES_USEFUL_FEATURES.md`
- **Статус версий:** `STATUS_REPORT.md`

---

## 🎯 Итог

**Три команды для счастья:**

```bash
# 1. Синхронизация
./sync-versions.sh all v5-full --force

# 2. Проверка
./check-consistency.sh

# 3. Коммит
git add . && git commit -m "🔄 [ALL] Синхронизация" && git push
```

**Время на синхронизацию всех версий:** ~2 минуты
**Экономия времени vs ручная синхронизация:** ~3 часа

---

**Создано:** 2026-01-08
**Статус:** ✅ Готово к использованию
